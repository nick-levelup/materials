/* ============= */
.children();
/*
Get the children of each element in the set of matched elements,
optionally filtered by a selector.
*/


/* ============= */
.find();
/*
Get the descendants of each element in the current set of matched
elements, filtered by a selector, jQuery object, or element.
*/


/* ============= */
.parent();
/*
Get the parent of each element in the current set of matched
elements, optionally filtered by a selector.
*/


/* ============= */
.parents();
/*
Get the ancestors of each element in the current set of
matched elements, optionally filtered by a selector.
*/


/* ============= */
.parentsUntil();
/*
Get the ancestors of each element in the current set of
matched elements, up to but not including the element matched
by the selector, DOM node, or jQuery object.
*/


/* ============= */
.offsetParent();
/*
Get the closest ancestor element that is positioned.
*/


/* ============= */
.closest();
/*
For each element in the set, get the first element that matches
the selector by testing the element itself and traversing up through
its ancestors in the DOM tree.
*/


/* ============= */
.next();
/*
Get the immediately following sibling of each element in the
set of matched elements. If a selector is provided, it retrieves
the next sibling only if it matches that selector.
*/


/* ============= */
.nextAll();
/*
Get all following siblings of each element in the set of matched
elements, optionally filtered by a selector.
*/


/* ============= */
.nextUntil();
/*
Get all following siblings of each element up to but not including
the element matched by the selector, DOM node, or jQuery object passed.
*/

/* ============= */
.prev();
/* ============= */
.prevAll();
/* ============= */
.prevUntil();


/* ============= */
.siblings();
/*
Get the siblings of each element in the set of matched
elements, optionally filtered by a selector.
*/